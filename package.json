{
  "name": "nbg",
  "version": "0.0.1",
  "author": "Anton Goryunov",
  "license": "MIT",
  "scripts": {
    "generate": "ts-node src/nbg.ts",
    "configure": "cd output && cmake-js configure",
    "build": "cd output && conan install . && cmake-js build",
    "make": "npm run generate && npm run build",
    "run": "ts-node test.ts",
    "makeAndRun": "npm run make && npm run run"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "fs-extra": "^8.1.0"
  },
  "devDependencies": {
    "@types/fs-extra": "^8.0.1",
    "node-addon-api": "^1.7.1",
    "ts-node": "^8.4.1",
    "typescript": "^3.7.2"
  }
}
